namespace SBC.Connected.Drive.Service.Domain.Core.UseCases.Health.Commands.UpdateBusinessHealth {
    using SBC.Domain.Commands;

    /// <summary>
    /// Interface defining the command to update the health state for a business
    /// </summary>
    public interface I<%= commandName %> : ICustomCommand<<%= commandName %>Request, <%= commandName %>Response> {
    }
}